node_modules/
# Generated by the check scripts. Regenerate with `npm run check` and `npm run shots`
# rather than committing screenshots and axe dumps.
results/
shots/
# Hook bookkeeping: which frontend files were touched and when. Local state, not shared.
.gate/
*.log

# Deliberately NOT ignored, because they are the baselines the checks compare against and
# they are only meaningful if they travel with the repository:
#   aria/        structural accessibility-tree baselines
#   budget.json  gzip ceilings
